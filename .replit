run = "npm start"
modules = ["nodejs-20"]
entrypoint = "server.js"

[deployment]
deploymentTarget = "autoscale"
build = ["npm", "install"]
run = ["npm", "start"]

[[ports]]
localPort = 3000
externalPort = 80
